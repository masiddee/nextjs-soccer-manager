import {Grid} from '@nextui-org/react';
import React from 'react';

const JoinTeam = () => {
  /**
   * 1) Get team ID and OTP from URL query (show error state if both aren't present in query params)
   * 2)
   */
  return (
    <Grid.Container lg>
      <div>Join Team Form will go here</div>
    </Grid.Container>
  );
};

export default JoinTeam;
